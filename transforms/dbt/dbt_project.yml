name: irrigation
version: "1.0.0"
config-version: 2

profile: irrigation

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  irrigation:
    staging:
      +materialized: view
    marts:
      +materialized: table