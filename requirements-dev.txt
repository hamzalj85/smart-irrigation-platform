# Development and CI tooling (not the runtime dependencies: those live in the
# requirements.txt of each component).
ruff==0.16.2
pytest>=8.0,<9

# Needed only to *import* the modules under test
paho-mqtt>=2.1.0
prometheus-client>=0.21,<1

# Analysis and modelling
duckdb>=1.0,<2
pandas>=2.2,<3
pyarrow>=17
scikit-learn>=1.5,<2
joblib>=1.4,<2
boto3>=1.34,<2

# Transformations
dbt-core==1.9.10
dbt-duckdb==1.9.6
