# Mosquitto
MOSQUITTO_ESP32_USER=esp32
MOSQUITTO_ESP32_PASSWORD=changeme
MOSQUITTO_BRIDGE_USER=bridge
MOSQUITTO_BRIDGE_PASSWORD=changeme

# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_TELEMETRY_TOPIC=irrigation.telemetry
KAFKA_DLQ_TOPIC=irrigation.telemetry.dlq

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme
MINIO_BUCKET=irrigation

# MongoDB
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=changeme

AIRFLOW_DB_PASSWORD=changeme
AIRFLOW_JWT_SECRET=changeme

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme